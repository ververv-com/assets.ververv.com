<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= name %> - <%= homepage.slogan %></title>
    <meta name="description" content="<%= homepage.sub_slogan %>">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            /* Convert hex to rgb for opacity manipulation */
            --primary-color: <%= homepage.theme_color || '#007AFF' %>;
            --primary-rgb: <%= (function(hex) {
                var result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex || '#007AFF');
                return result ? parseInt(result[1], 16) + ", " + parseInt(result[2], 16) + ", " + parseInt(result[3], 16) : "0, 122, 255";
            })(homepage.theme_color) %>;
            
            --bg-color: #ffffff;
            --bg-secondary: #f5f5f7;
            
            /* Alternating backgrounds for better contrast */
            --bg-hero: radial-gradient(circle at 50% 0%, rgba(var(--primary-rgb), 0.12) 0%, transparent 70%), #f5f5f7;
            --bg-features: #ffffff;
            --bg-support: #f5f5f7;
            --bg-footer: #1d1d1f;
            
            --text-primary: #1d1d1f;
            --text-secondary: #86868b;
            --text-footer: #86868b;
            --text-footer-hover: #ffffff;
            --border-color: #d2d2d7;
            
            --radius-large: 28px;
            --radius-medium: 16px;
            --max-width: 1080px;
            
            --shadow-sm: 0 2px 8px rgba(0,0,0,0.04);
            --shadow-md: 0 12px 24px rgba(0,0,0,0.08);
            --shadow-lg: 0 24px 48px rgba(0,0,0,0.12);
            --shadow-glow: 0 0 80px rgba(var(--primary-rgb), 0.15);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-font-smoothing: antialiased;
        }

        body {
            font-family: "SF Pro Display", "Inter", -apple-system, BlinkMacSystemFont, "Helvetica Neue", Helvetica, Arial, sans-serif;
            background-color: var(--bg-color);
            color: var(--text-primary);
            line-height: 1.5;
            font-weight: 400;
            letter-spacing: -0.01em;
            overflow-x: hidden;
        }

        /* Animations */
        @keyframes fadeUp {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .animate-fade-up {
            opacity: 0;
            animation: fadeUp 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
        }

        .delay-100 { animation-delay: 0.1s; }
        .delay-200 { animation-delay: 0.2s; }
        .delay-300 { animation-delay: 0.3s; }

        /* Typography */
        h1, h2, h3 {
            font-weight: 700;
            letter-spacing: -0.02em;
            color: var(--text-primary);
        }

        a {
            text-decoration: none;
            color: var(--primary-color);
            transition: all 0.2s ease;
        }

        /* Layout */
        .container {
            max-width: var(--max-width);
            margin: 0 auto;
            padding: 0 24px;
        }

        /* Navbar */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: saturate(180%) blur(20px);
            -webkit-backdrop-filter: saturate(180%) blur(20px);
            border-bottom: 1px solid rgba(0,0,0,0.05);
            height: 64px;
            display: flex;
            align-items: center;
            transition: background 0.3s;
        }

        .navbar-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            max-width: var(--max-width);
            margin: 0 auto;
            padding: 0 24px;
        }

        .nav-title {
            font-size: 18px;
            font-weight: 700;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }
        
        .nav-title img {
            width: 28px;
            height: 28px;
            border-radius: 6px;
        }

        .nav-download {
            background: var(--text-primary);
            color: white;
            padding: 8px 20px;
            border-radius: 100px;
            font-size: 14px;
            font-weight: 600;
            transition: transform 0.2s, opacity 0.2s;
        }
        
        .nav-download:hover {
            transform: scale(1.02);
            opacity: 0.9;
            color: white;
        }

        .nav-link {
            font-size: 14px;
            font-weight: 500;
            color: var(--text-secondary);
            margin-right: 24px;
            transition: color 0.2s;
        }

        .nav-link:hover {
            color: var(--text-primary);
        }

        .nav-right {
            display: flex;
            align-items: center;
        }

        /* Hero */
        .hero {
            padding: 120px 0 60px;
            text-align: center;
            position: relative;
            overflow: hidden;
            background: var(--bg-hero);
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 800px;
            margin: 0 auto;
        }

        .app-icon-wrapper {
            margin-bottom: 40px;
            display: inline-block;
            position: relative;
        }

        .app-icon {
            width: 140px;
            height: 140px;
            border-radius: 32px;
            box-shadow: var(--shadow-lg);
            position: relative;
            z-index: 2;
            background: white;
        }
        
        .app-icon-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 160px;
            height: 160px;
            background: var(--primary-color);
            filter: blur(60px);
            opacity: 0.3;
            z-index: 1;
            border-radius: 50%;
        }

        .hero-title {
            font-size: 64px;
            line-height: 1.05;
            font-weight: 800;
            margin-bottom: 24px;
            background: linear-gradient(135deg, var(--text-primary) 0%, #4a4a4a 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-subtitle {
            font-size: 24px;
            line-height: 1.4;
            font-weight: 500;
            color: var(--text-secondary);
            margin-bottom: 48px;
            max-width: 640px;
            margin-left: auto;
            margin-right: auto;
        }

        .app-store-badge {
            height: 56px;
            transition: transform 0.2s, box-shadow 0.2s;
            border-radius: 10px;
        }
        
        .app-store-badge:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        /* Features */
        .features {
            padding: 80px 0 120px;
            background: var(--bg-features);
        }

        .feature-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 120px;
            gap: 80px;
        }
        
        .feature-item:last-child {
            margin-bottom: 0;
        }

        .feature-item:nth-child(even) {
            flex-direction: row-reverse;
        }

        .feature-content {
            flex: 1;
            max-width: 480px;
        }

        .feature-image-container {
            flex: 1.2;
            display: flex;
            justify-content: center;
            position: relative;
        }
        
        .feature-image-container::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80%;
            height: 80%;
            background: radial-gradient(circle, rgba(var(--primary-rgb), 0.05) 0%, transparent 70%);
            z-index: 0;
        }

        .feature-image {
            max-width: 100%;
            border-radius: var(--radius-large);
            box-shadow: var(--shadow-lg);
            position: relative;
            z-index: 1;
            border: 1px solid rgba(0,0,0,0.03);
        }

        .feature-label {
            font-size: 14px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            color: var(--primary-color);
            margin-bottom: 16px;
            display: block;
        }

        .feature-title {
            font-size: 40px;
            line-height: 1.1;
            font-weight: 700;
            margin-bottom: 24px;
        }

        .feature-desc {
            font-size: 20px;
            line-height: 1.5;
            font-weight: 400;
            color: var(--text-secondary);
        }

        /* Support / FAQ */
        .support {
            padding: 120px 0;
            background: var(--bg-support);
            border-top: 1px solid rgba(0,0,0,0.05);
        }

        .section-header {
            text-align: center;
            margin-bottom: 80px;
        }

        .section-title {
            font-size: 40px;
            font-weight: 700;
            margin-bottom: 16px;
        }
        
        .section-subtitle {
            font-size: 20px;
            color: var(--text-secondary);
        }

        .faq-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(340px, 1fr));
            gap: 32px;
            max-width: 960px;
            margin: 0 auto;
        }
        
        .faq-item {
            background: #fff;
            padding: 32px;
            border-radius: var(--radius-medium);
            box-shadow: var(--shadow-sm);
            transition: transform 0.2s, box-shadow 0.2s;
        }
        
        .faq-item:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-md);
        }

        .faq-item h3 {
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--text-primary);
        }

        .faq-item p {
            font-size: 16px;
            color: var(--text-secondary);
            line-height: 1.6;
        }
        
        .contact-support {
            text-align: center;
            margin-top: 80px;
        }
        
        .contact-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 32px;
            background: white;
            border: 1px solid var(--border-color);
            border-radius: 100px;
            color: var(--text-primary);
            font-weight: 600;
            font-size: 16px;
            transition: all 0.2s;
        }
        
        .contact-btn:hover {
            background: var(--bg-secondary);
            border-color: var(--text-secondary);
        }

        /* Footer */
        .footer {
            background: var(--bg-footer);
            padding: 60px 0;
            color: var(--text-footer);
            border-top: none;
        }

        .footer-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        
        .footer-left {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .footer-logo {
            display: flex;
            align-items: center;
            gap: 10px;
            color: #fff;
            font-weight: 600;
            font-size: 16px;
        }

        .footer-logo img {
            width: 24px;
            height: 24px;
            border-radius: 6px;
        }
        
        .copyright {
            font-size: 12px;
            color: var(--text-footer);
            opacity: 0.6;
        }

        .footer-links {
            display: flex;
            gap: 24px;
        }

        .footer-links a {
            font-size: 13px;
            color: var(--text-footer);
            font-weight: 500;
            transition: color 0.2s;
        }
        
        .footer-links a:hover {
            color: var(--text-footer-hover);
        }

        /* Mobile */
        @media (max-width: 768px) {
            .hero { padding: 100px 0 60px; }
            .hero-title { font-size: 40px; }
            .hero-subtitle { font-size: 20px; }
            
            .feature-item {
                flex-direction: column;
                text-align: center;
                gap: 48px;
                margin-bottom: 80px;
            }
            
            .feature-item:nth-child(even) {
                flex-direction: column;
            }
            
            .feature-content { max-width: 100%; }
            
            .footer-content {
                flex-direction: column-reverse;
                gap: 24px;
                text-align: center;
            }
            
            .footer-left {
                align-items: center;
            }
            
            .footer-links {
                flex-direction: column;
                align-items: center;
                gap: 16px;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar">
        <div class="navbar-content">
            <a href="#" class="nav-title">
                <img src="../../assets/<%= homepage.app_icon %>" alt="<%= name %>">
                <%= name %>
            </a>
            <div class="nav-right">
                <a href="#support" class="nav-link">FAQ</a>
                <a href="<%= homepage.app_store_url %>" class="nav-download">Download</a>
            </div>
        </div>
    </nav>

    <!-- Hero -->
    <section class="hero">
        <div class="container hero-content">
            <div class="app-icon-wrapper animate-fade-up">
                <div class="app-icon-glow"></div>
                <img src="../../assets/<%= homepage.app_icon %>" alt="<%= name %> Icon" class="app-icon">
            </div>
            
            <h1 class="hero-title animate-fade-up delay-100"><%= homepage.slogan %></h1>
            <p class="hero-subtitle animate-fade-up delay-200"><%= homepage.sub_slogan %></p>
            
            <div class="animate-fade-up delay-300">
                <a href="<%= homepage.app_store_url %>" target="_blank" style="display: inline-block;">
                    <img src="../../assets/common/app-store-badge.svg" alt="Download on the App Store" class="app-store-badge">
                </a>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section class="features">
        <div class="container">
            <% homepage.features.forEach(function(feature, index) { %>
            <div class="feature-item">
                <div class="feature-content">
                    <span class="feature-label">Feature <%= index + 1 %></span>
                    <h2 class="feature-title"><%= feature.title %></h2>
                    <p class="feature-desc"><%= feature.description %></p>
                </div>
                <div class="feature-image-container">
                    <img src="../../assets/<%= feature.image %>" alt="<%= feature.title %>" class="feature-image">
                </div>
            </div>
            <% }); %>
        </div>
    </section>

    <!-- Support / FAQ -->
    <section id="support" class="support">
        <div class="container">
            <div class="section-header">
                <h2 class="section-title">Frequently Asked Questions</h2>
                <p class="section-subtitle">Everything you need to know about the app.</p>
            </div>
            
            <div class="faq-grid">
                <% homepage.faqs.forEach(function(faq) { %>
                <div class="faq-item">
                    <h3><%= faq.question %></h3>
                    <p><%= faq.answer %></p>
                </div>
                <% }); %>
            </div>

            <div class="contact-support">
                <a href="mailto:<%= email %>?subject=<%= homepage.support_email_subject || 'Support Request' %>" class="contact-btn">
                    Contact Support
                </a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer class="footer">
        <div class="container footer-content">
            <div class="footer-left">
                <div class="footer-logo">
                    <img src="../../assets/<%= homepage.app_icon %>" alt="<%= name %>">
                    <span><%= name %></span>
                </div>
                <div class="copyright">
                    &copy; <%= new Date().getFullYear() %> <%= homepage.company_name || name %>. All rights reserved.
                </div>
            </div>
            
            <div class="footer-links">
                <a href="../privacy/">Privacy Policy</a>
                <% if (pages.includes('terms')) { %>
                <a href="../terms/">Terms of Use</a>
                <% } %>
                <a href="mailto:<%= email %>">Contact</a>
            </div>
        </div>
    </footer>

</body>
</html>
